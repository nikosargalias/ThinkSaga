class Article {
  constructor({ title, author, body, id, category }) {
    this.title = title;
    this.author = author;
    this.body = body;
    this.id = id;
    this.category = category;
  }
}

export default Article;
